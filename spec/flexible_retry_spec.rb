require 'spec_helper'

describe FlexibleRetry do
  it 'has a version number' do
    expect(FlexibleRetry::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
